spring.application.name=backend-springboot
spring.datasource.url=jdbc:mysql://localhost:3306/tasklist?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=12345678

# show all queries (during development)
# in production it is better to disable the params, as performance drops due to constant writing to the file system
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

